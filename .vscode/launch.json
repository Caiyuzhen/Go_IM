{
	// 👇配置 golang 的 debug
	"version": "0.2.0",
	"configurations": [
		{
			"name": "golang",
			"type": "go",
			"request": "launch",
			"mode": "auto",
			// 当运行单个文件时, {workspaceFolder} 会被替换为当前文件所在目录
			"program": "${workspaceFolder}",
			"env": {},
			"args": []
		}
	]
}